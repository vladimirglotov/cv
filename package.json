{
  "name": "resume",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://vladimirglotov.github.io/resume/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "rm -rf dist && vite build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^4.1.0"
  }
}
